// components/FormBuilder/FormBuilderContext.js — Context definition (separate file to avoid circular imports)
import { createContext } from 'react';

export const FormBuilderContext = createContext(null);
